spring:
  application:
    name: currency-rate-gif-selection-app-test

#  datasource:
#    url: jdbc:h2:mem:testdb;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver

  liquibase:
    enabled: false  # Отключаем Liquibase для тестов

  jpa:
    hibernate.ddl-auto: none
    show-sql: true
    database-platform: org.hibernate.dialect.H2Dialect

external-giphy:
  api-key: test-giphy-key
  url: http://dont-call-this-in-tests.com
  search-word:
    positive-rate: rich
    negative-rate: broke
  search-limit: 25

external-exchangerate:
  url: http://dont-call-this-in-tests.com

#logging:
#  level:
#    root: WARN
#    ru.yandex.kingartaved.currencyrategifselectionapp: DEBUG